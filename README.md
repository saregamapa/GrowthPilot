# GrowthPilot / BrokerBoost site

Static landing page: `index.html` and related assets.

## Deploy on Render

`render.yaml` defines a static site service. Connect the repo and deploy, or use Render Blueprints.
